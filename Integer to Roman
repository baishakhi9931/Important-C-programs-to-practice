/* I have used arrays of integer values and strings for Roman numeral symbols.
I have used the malloc function to allocate memory for the result string.
I have used the strcat function to concatenate the Roman numeral symbols to the result string.
I have used a for loop instead of a while loop to iterate through the values array. */









char *intToRoman(int num) {
    // Initialize arrays for integer values and corresponding Roman numeral symbols
    int values[] = {1000, 900, 500, 400, 100, 90, 50, 40, 10, 9, 5, 4, 1};
    char *romanNumerals[] = {"M", "CM", "D", "CD", "C", "XC", "L", "XL", "X", "IX", "V", "IV", "I"};

    // Create a string to store the result
    char *result = (char*) malloc(sizeof(char) * 16);
    int index = 0;

    // Iterate until the given number becomes zero
    for (int i = 0; num > 0; i++) {
        // Check if the current value can be subtracted from the given number
        while (num >= values[i]) {
            // Append the corresponding Roman numeral symbol to the result string
            strcat(result, romanNumerals[i]);
            index += strlen(romanNumerals[i]);
            // Subtract the value from the number
            num -= values[i];
        }
    }

    // Return the result string
    return result;
}
